name: Deploy to Production

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        
    - name: Install dependencies
      run: npm install
      
    - name: Deploy to server
      env:
        GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
        SEARCHAPI_KEY: ${{ secrets.SEARCHAPI_KEY }}
      run: |
        echo "Deploying with Google API Key from GitHub secrets..."
        # Aqui você coloca o comando para fazer deploy
        # Por exemplo, se estiver usando Railway, Heroku, etc.
