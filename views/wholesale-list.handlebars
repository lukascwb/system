<!DOCTYPE html>
<html>
<head>
    <title>Wholesale Upload Batches</title>
    <link rel="stylesheet" href="/css/style.css"> <!-- Or your main stylesheet -->
    <style>
        .list-group-item { display: flex; justify-content: space-between; align-items: center; }
    </style>
</head>
<body>
    <h1>Wholesale Upload Batches</h1>

    {{!-- Include your navigation partial here if you have one --}}
    {{!-- {{> navbar page='wholesale_list'}} --}}

    {{#if wholesaleIds}}
        <ul class="list-group">
             {{#each wholesaleIds}}
                <li class="list-group-item">
                    <span>{{this}}</span>
                    {{!-- Link to view the details for this wholesale_id --}}
                    <a href="/wholesale-products-by-id/{{this}}" class="btn btn-sm btn-outline-primary">View Details</a>
                </li>
            {{/each}}
        </ul>
    {{else}}
        <p>No wholesale products have been uploaded yet.</p>
    {{/if}}

    <p><a href="/wholesale">Upload New Wholesale CSV</a></p>
    <p><a href="/list">Back to Keepa List</a></p>
    <p><a href="/">Go to Home</a></p>

</body>
</html>